import { CalculatorView } from "@/presentation/features/products/calculator-view";

export const metadata = { title: "الحاسبة" };

export default function CalculatorPage() {
  return <CalculatorView />;
}
